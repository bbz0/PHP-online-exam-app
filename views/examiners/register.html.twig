{% extends 'inc/base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
	<section class="py-5">
		<div class="container">
			<div class="row">

				{% block msgs %}
					{{ parent() }}
				{% endblock %}

				<div class="col-md-6 mx-auto">
					<h1 class="mb-5">{{ title }}</h1>
					<form action="{{ constant('URLROOT') }}/examiners/register" method="post" onsubmit="return register.register()">
						<div class="card">
							<div class="card-body">
								<div class="row">
									<div class="col-md-6">
										<div class="form-group">
											<label for="firstName">First Name: <sup>*</sup></label>
											<input class="form-control input--firstName {% if examinerData.error.firstName %}is-invalid{% endif %}" type="text" name="firstName" id="firstName" value="{{ examinerData.firstName }}" maxlength="50">
											<span class="invalid-feedback">{{ examinerData.error.firstName }}</span>
										</div>
									</div>
									<div class="col-md-6">
										<div class="form-group">
											<label for="firstName">Last Name: <sup>*</sup></label>
											<input class="form-control input--lastName {% if examinerData.error.lastName %}is-invalid{% endif %}" type="text" name="lastName" id="lastName" value="{{ examinerData.lastName }}" maxlength="50">
											<span class="invalid-feedback">{{ examinerData.error.lastName }}</span>
										</div>
									</div>
								</div>
								<div class="form-group">
									<label for="username">Username: <sup>*</sup></label>
									<input class="form-control input--username {% if examinerData.error.username %}is-invalid{% endif %}" type="text" name="username" id="username" value="{{ examinerData.username }}" maxlength="50">
									<span class="invalid-feedback">{{ examinerData.error.username }}</span>
								</div>
								<div class="form-group">
									<label for="password">Password: <sup>*</sup></label>
									<input class="form-control input--password {% if examinerData.error.password %}is-invalid{% endif %}" type="password" name="password" id="password" value="{{ examinerData.password }}" maxlength="50">
									<span class="invalid-feedback">{{ examinerData.error.password }}</span>
								</div>
								<div class="form-group">
									<label for="confirmPassword">Confirm Password: <sup>*</sup></label>
									<input class="form-control input--confirm {% if examinerData.error.confirmPassword %}is-invalid{% endif %}" type="password" name="confirmPassword" id="confirmPassword" value="{{ examinerData.confirmPassword }}" maxlength="50">
									<span class="invalid-feedback">{{ examinerData.error.confirmPassword }}</span>
								</div>
								<input type="submit" class="btn btn-success btn-lg register--btn" name="submit" value="Register">	
							</div>
						</div>
					</form>
				</div>
			</div>
		</div>
	</section>
{% endblock %}

{% block javascript %}
	<script type="text/javascript" src="{{ constant('URLROOT') }}/js/register.js"></script>
{% endblock %}